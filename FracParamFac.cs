/*
namespace ooopsem;
public static class FractalParamsFactory
{
    public static Fractalparams Create(string fractalType)
    {
        switch (fractalType)
        {
            case "Mandelbrot":
                return new Fractalparams { ReStart = -2.0, ReEnd = 1.0, ImStart = -1.2, ImEnd = 1.2 };
            case "Julia":
                return new Fractalparams { ReStart = -1.0, ReEnd = 1.0, ImStart = -1.2, ImEnd = 1.2 };
            default:
                throw new ArgumentException("Invalid fractal type");
        }
    }
}


*/